<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local emulator builds may call the loopback-only HTTP Compose stack. -->
    <application android:usesCleartextTraffic="true" />
</manifest>
